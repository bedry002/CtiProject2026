DT_API_KEY=odt_4yyy6QDX_gZHGQKfSqvzrF4mGyHnUtkoPHQPwHpNC
DT_BASE_URL=http://192.168.4.38:1010

# Ollama (post-scoring LLM enrichment). Set OLLAMA_ENABLED=false to skip the
# stage entirely (e.g. when Ollama isn't installed). Threshold is on the 0–1
# confidence scale; 0.25 = BAND_MEDIUM, which gates roughly to the events an
# analyst would actually look at.
OLLAMA_ENABLED=true
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.1:8b
OLLAMA_THRESHOLD=0.25
