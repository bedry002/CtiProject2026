# Core pipeline
pymisp>=2.4

# NER (Stage 2) — uncomment when wiring up
# spacy>=3.7
# pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.7.3/en_core_web_trf-3.7.3-py3-none-any.whl

# LLM enrichment (Stage 3) — talks to a local Ollama daemon over HTTP.
# No extra Python deps required; uses the existing `requests` install.

# Sbom integration
requests>=2.31.0
python-dotenv>=1.0.0
